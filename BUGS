
    ~~~~~ [SOLVED] ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    1. `PRINT` macro is not working.

    solution:
    make sure operands match variable sizes, if in the length defintions you use 'long', then
    use movl to set a register to such length value, if you use movq, make sure you defined the
    lengths values with 'quad'

